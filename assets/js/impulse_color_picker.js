(function( $ ) {
    $(function() {
         
        // Add Color Picker to all inputs that have 'color-field' class
        $( '.impulse_plugin-color-picker' ).wpColorPicker();
         
    });
})( jQuery );